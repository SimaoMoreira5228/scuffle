[package]
name = "scuffle-settings"
version = "0.1.4"
authors = ["Scuffle <opensource@scuffle.cloud>"]
documentation = "https://docs.rs/scuffle-settings"
edition = "2024"
keywords = ["settings", "configuration", "environment", "config"]
license = "MIT OR Apache-2.0"
readme = "README.md"
repository = "https://github.com/scufflecloud/scuffle"
description = "Tools for managing configuration from environment variables or config files."

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage_nightly)'] }

[features]
## Enables cli parsing using clap
cli = ["clap"]
## Enables the ron format
ron = ["config/ron"]
## Enables the toml format
toml = ["config/toml"]
## Enables the yaml format
yaml = ["config/yaml"]
## Enables the json format
json = ["config/json"]
## Enables the json5 formast
json5 = ["config/json5"]
## Enables the ini format
ini = ["config/ini"]
## Enables all formats
all-formats = ["toml", "json", "yaml", "json5", "ini", "ron"]
## Enables templating support via jinja
templates = ["minijinja"]
## Enables scuffle-bootstrap support
bootstrap = ["scuffle-bootstrap", "anyhow", "cli"]
## Enables everything
full = ["all-formats", "templates", "cli", "bootstrap"]
## Enables changelog and documentation of feature flags
docs = ["dep:scuffle-changelog", "dep:document-features"]

[[example]]
name = "scuffle-settings-cli"
path = "examples/cli.rs"
required-features = ["cli"]

[dependencies]
anyhow = { optional = true, version = "1" }
clap = { optional = true, version = "4" }
config = { default-features = false, version = "0.15.18" } # at least 0.15.18 because the parse_empty test fails otherwise
document-features = { optional = true, version = "0.2" }
minijinja = { features = ["custom_syntax", "json", "urlencode"], optional = true, version = "2" }
scuffle-bootstrap = { optional = true, path = "../bootstrap", version = "0.1" }
scuffle-changelog = { optional = true, path = "../changelog", version = "0.1" }
serde = "1"
serde_derive = "1"
thiserror = "2"

[dev-dependencies]
# For examples:
serde_derive = "1"
smart-default = "0.7"

[package.metadata.xtask.powerset]
additive-features = [
    "cli",
    "ron",
    "toml",
    "yaml",
    "json",
    "json5",
    "ini",
    "templates",
    "all-formats",
    "full",
    "bootstrap",
    "docs",
]

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
    "--cfg",
    "docsrs",
    "--sort-modules-by-appearance",
    "--generate-link-to-definition",
]

[package.metadata.sync-readme.rustdoc-mappings]
changelog = "./CHANGELOG.md"
scuffle_bootstrap = "../bootstrap"

[package.metadata.sync-readme.badges]
docs-rs = true
crates-io = true
license = true
codecov = true
