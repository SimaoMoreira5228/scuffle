<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scuffle Core Email Template Preview</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,100..900&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            margin: 2rem;
        }

        * {
            box-sizing: border-box;
        }

        .bold {
            font-weight: 700;
        }
    </style>
</head>

<body>
    <a href="/">< Back</a>

    <p>
        <span class="bold">From:</span>
        <span><%= self.from_name %> &lt;<%= self.from_address %>&gt;</span>
        <br>
        <span class="bold">To:</span>
        <span><%= self.to_name %> &lt;<%= self.to_address %>&gt;</span>
        <br>
        <span class="bold">Subject:</span>
        <span><%= self.subject %></span>
    </p>

    <p class="bold">Text version:</p>
    <pre style="border: 1px solid black; width: 600px; white-space: pre-wrap; padding: 0.5rem;"><%- self.text %></pre>

    <p class="bold">HTML version:</p>
    <iframe src="/<%= self.name %>/html.html" style="width: 600px; height: 800px; border: 1px solid black;"></iframe>
</body>

</html>
