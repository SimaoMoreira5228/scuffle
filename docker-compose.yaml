services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: scuffle
      POSTGRES_PASSWORD: scuffle
    ports:
      - 127.0.0.1:5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
  redis:
    image: redis:latest
    ports:
      - 127.0.0.1:6379:6379

volumes:
  postgres_data:
